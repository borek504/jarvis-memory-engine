# Release boundary

This public repository is a sanitized export of the Memory subsystem. It must never contain live databases, private memory records, credentials, account identifiers, user-specific absolute paths, raw transcripts, or private runtime logs.
