# Release process

Public releases are prepared from synthetic/sanitized source only. Before code is published, the release tree must pass unit tests, syntax checks, and the repository privacy scanner. A release is blocked by any CRITICAL or HIGH security/privacy finding.
